import React from 'react';
import ReactDOM from 'revact-dom/client';
import App from './App.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <App />v
  </React.StrictMode>
);
